<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="LoginPage.Views.AddContactPage">
    <ContentPage.Content>
        <StackLayout>
            <Entry Text="{Binding contact.Name}" Placeholder="Name"/>
            <Entry Text="{Binding contact.PhoneNumber}" Placeholder="Phone number"/>
            <StackLayout VerticalOptions="CenterAndExpand">
                
            </StackLayout>
            <Button Text="Add" VerticalOptions="EndAndExpand" HorizontalOptions="FillAndExpand" 
                    Command="{Binding sendUser}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>