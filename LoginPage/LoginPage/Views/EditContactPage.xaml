<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="LoginPage.Views.EditContactPage" Title="Edit">
    <ContentPage.Content>
        <StackLayout>
            <Entry Text="{Binding contact.Name}" Placeholder="Name"/>
            <Entry Text="{Binding contact.PhoneNumber}" Placeholder="Phone number"/>
            <StackLayout VerticalOptions="CenterAndExpand">

            </StackLayout>
            <Button Text="Save" VerticalOptions="EndAndExpand" HorizontalOptions="FillAndExpand" BackgroundColor="Black"
                    TextColor="White" Command="{Binding editItem}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>